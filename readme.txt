1. added basic 2 activities (one with listview, one with google map). both working. 
2. added custom layout to homepage listview.  added floating action button for new consignment. working correctly.
